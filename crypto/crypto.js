const password = prompt('give me your password')
password = "password"
function crypto(password) {
	const [letOne, letTwo, letThree, letFour, letFive, letSix, letSeven, letEight] = password
	for (let i = 0; i < password.length; i++) {
		const index = password.indexOf(password[])
    if (index !== -1) {
        const transfer = tasks.splice(index, 1) 
        tasks.unshift(transfer[0])
		
	}
     

    }



	console.log(password);
}
crypto(password)
